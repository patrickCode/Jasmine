export class ConsoleLogger {
    LogConsoleMessage(message) {
        console.log(message);
    }

    LogWarning(message) {
        console.warn(message);
    }

    LogError(message) {
        console.error(message);
    }
}